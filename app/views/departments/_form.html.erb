<%= form_with(model: department, local: true, html: {class: "form-horizontal center"}) do |d| %>
  <% if department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="form-group">
    <%= d.label :name, "Name:", class: "col-md-4 control-label" %>
    <div class="col-md-5">
      <%= d.text_field :name, class: "form-control" %>
    </div>
  </div>
  
  <div class="form-group">
    <%= d.label :description, "Description:", class: "col-md-4 control-label" %>
    <div class="col-md-8">
      <%= d.text_area :description, class: "form-control", size: "10x6" %>
    </div>
  </div>

  <div class="form-group">
    <%= d.label :company_id, "Company Id:", class: "col-md-4 control-label" %>
    <div class="col-md-8">
      <%= d.text_area :company_id, class: "form-control", size: "10x6" %>
    </div>
  </div>
  
  <div class="actions">
    <%= d.submit "Submit", class: "btn btn-outline-info my-2 my-sm-0 offset-md-3" %>
  </div>
<% end %>